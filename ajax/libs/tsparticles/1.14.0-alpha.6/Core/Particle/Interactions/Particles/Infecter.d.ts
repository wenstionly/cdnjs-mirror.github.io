import { Particle } from "../../../Particle";
import { Container } from "../../../Container";
export declare class Infecter {
    static infect(p1: Particle, container: Container, delta: number): void;
}
